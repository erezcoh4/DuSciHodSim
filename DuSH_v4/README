open src/*/*.cpp src/*.cpp include/*/*.hpp Makefile -a xcode


v3 Dec-6, 2019
Changes from v1:
------------------
1) Added a photon as TPolyLine3D


v2 Dec-2, 2019
Changes from v1:
------------------
1) compiled with ROOT cern libraries using 
	LDFLAGS += -L$(shell root-config --libdir) -lEG -lSpectrum -lHist

2) added version.hpp
